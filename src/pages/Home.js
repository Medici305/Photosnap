import React from 'react';
import styled from 'styled-components';
// Components
import Top from '../components/Top';

const Home = ({ menu }) => {
    return (
        <div>
            <Top />
        </div>
    )
}

export default Home
